import styled from "styled-components";

export const UploadedFileText = styled.p`
  font-size: 0.8rem;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
  max-width: 50%;
  color: black;
`;
